import { BadRequestException, Body, Controller, Delete, Get, Param, Patch, Post, Req, UseGuards, UseInterceptors } from '@nestjs/common'; import { z } from 'zod'; import { AuthGuard } from '../common/guards/auth.guard'; import { ScopedRequest, UserScopeInterceptor } from '../common/interceptors/user-scope.interceptor'; import { EmergencyPreparednessService } from './emergency-preparedness.service';
const item = z.object({ category: z.enum(['FOOD', 'WATER', 'FIRST_AID', 'IMPORTANT_DOCUMENTS', 'EVACUATION_PLAN', 'OTHER']), name: z.string().trim().min(1).max(120), reviewDate: z.string().date().nullable().optional(), notes: z.string().trim().max(1000).nullable().optional() });
@Controller('emergency-preparedness') @UseGuards(AuthGuard) @UseInterceptors(UserScopeInterceptor) export class EmergencyPreparednessController { constructor(private readonly service: EmergencyPreparednessService) {} @Get() list(@Req() req: ScopedRequest) { return this.service.list(req.userId!); } @Post() create(@Req() req: ScopedRequest, @Body() body: unknown) { const parsed = item.safeParse(body); if (!parsed.success) throw new BadRequestException(parsed.error.flatten().fieldErrors); return this.service.create(req.userId!, parsed.data); } @Patch(':id') update(@Req() req: ScopedRequest, @Param('id') id: string, @Body() body: unknown) { const parsed = item.partial().extend({ isComplete: z.boolean().optional() }).safeParse(body); if (!parsed.success) throw new BadRequestException(parsed.error.flatten().fieldErrors); return this.service.update(req.userId!, id, parsed.data); } @Delete(':id') remove(@Req() req: ScopedRequest, @Param('id') id: string) { return this.service.remove(req.userId!, id); } }
