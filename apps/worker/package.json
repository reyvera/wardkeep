{
  "name": "@wardkeep/worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "start": "node dist/main.js",
    "dev": "ts-node src/main.ts",
    "test": "vitest run",
    "lint": "eslint src/",
    "postinstall": "echo 'Run npm install from the monorepo root to install all dependencies'"
  },
  "dependencies": {
    "@wardkeep/shared": "workspace:*",
    "@wardkeep/finance-engine": "workspace:*",
    "@wardkeep/ai-engine": "workspace:*",
    "@wardkeep/importers": "workspace:*",
    "bullmq": "^5.0.0",
    "ioredis": "^5.3.0",
    "@prisma/client": "^5.14.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
